{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Technical Manual \u2014 LunarPhaseORM","text":"<p>LunarPhaseORM is an async-first Object-Relational Mapping (ORM) framework for Python 3.9+ accelerated by a native C-extension compiled from Rust (<code>_lunarphase_rs</code>).</p> <p>The framework combines an Active Record interface (<code>model.save()</code>, <code>Model.create()</code>) with a Data Mapper architecture utilizing a Unit of Work transaction engine, an Identity Map, and an event-loop micro-task queue for relationship batching.</p>"},{"location":"#technical-overview","title":"Technical Overview","text":"<pre><code>+-------------------------------------------------------------------+\n|                        Python Application                         |\n|  - Async Engine Driver (aiosqlite / asyncpg / aiomysql)           |\n|  - Model Classes (ModelBase Metaclass &amp; Field Descriptors)        |\n|  - Identity Map &amp; UnitOfWork Transaction Management               |\n|  - DeferredAutoBatcher Micro-Task Loop Scheduler                  |\n+-------------------------------------------------------------------+\n                                 \u2502\n           PyO3 / C-API FFI Boundary (abi3-py39)\n                                 \u2502\n                                 \u25bc\n+-------------------------------------------------------------------+\n|               Native Rust Extension (_lunarphase_rs)              |\n|  - StateTracker (FxHashMap&lt;String, FieldEntry&gt; Snapshot)          |\n|  - BatchAggregator (FxHashSet&lt;i64&gt; &amp; FxHashSet&lt;String&gt;)           |\n|  - QueryCompiler (Pre-allocated String Buffers &amp; Dialects)        |\n|  - SchemaDiffEngine (Model vs Database DDL Comparison)            |\n+-------------------------------------------------------------------+\n</code></pre>"},{"location":"#architectural-objectives","title":"Architectural Objectives","text":"<ol> <li>Memory Allocation Efficiency: Model snapshot tracking and dirty field comparisons are offloaded to C-contiguous Rust memory structures (<code>FxHashMap</code>), avoiding per-instance Python dictionary graph creation.</li> <li>N+1 Query Resolution: Relationship queries are deferred and batched into single <code>WHERE id IN (...)</code> operations via <code>asyncio.get_running_loop().call_soon()</code>.</li> <li>Selective Field Updates: Dirty attribute tracking ensures SQL <code>UPDATE</code> statements format only fields modified since the last snapshot hydration.</li> <li>C-API Compatibility: Rust modules are compiled using standard <code>PyO3</code> bindings under Python C-API ABI3 specifications (<code>cp39-abi3</code>), enabling binary wheel execution across CPython 3.9 through 3.14+.</li> </ol>"},{"location":"#module-breakdown","title":"Module Breakdown","text":"Module Path Primary Responsibility Core Classes / Types <code>lunarphase.core.model</code> Metaclass attribute registration and Active Record API <code>ModelBase</code>, <code>Model</code> <code>lunarphase.core.fields</code> Attribute descriptors and AST expression construction <code>FieldDescriptor</code>, <code>PrimaryKeyField</code>, <code>StringField</code>, <code>IntegerField</code> <code>lunarphase.core.state</code> Snapshot management and dirty field identification <code>StateTracker</code> (Rust bridge), <code>PyStateTracker</code> (Fallback) <code>lunarphase.query.builder</code> AST compilation and parameter binding <code>QueryBuilder</code>, <code>ColumnRef</code>, <code>BinaryOp</code> <code>lunarphase.query.batcher</code> Event loop micro-task batching scheduler <code>DeferredAutoBatcher</code>, <code>BatchAggregator</code> <code>lunarphase.core.uow</code> Transaction boundaries and identity mapping <code>UnitOfWork</code>, <code>IdentityMap</code> <code>lunarphase.migrations</code> Schema extraction, diffing, and CLI execution <code>SchemaDiffEngine</code>, <code>MigrationRunner</code>, <code>CLI</code>"},{"location":"architecture/","title":"Core Architecture","text":"<p>This document describes the internal design of LunarPhaseORM, including the PyO3 C-extension boundary, Rust memory layouts, state isolation mechanisms, and fallback systems.</p>"},{"location":"architecture/#c-extension-interoperability-_lunarphase_rs","title":"C-Extension Interoperability (<code>_lunarphase_rs</code>)","text":"<p>The core execution engine uses a native C-extension compiled from Rust via PyO3. The module is registered under the standard Python C-API ABI3 framework (<code>abi3-py39</code>), permitting a single compiled binary wheel to execute across CPython versions 3.9 through 3.14+.</p> <pre><code>+-------------------------------------------------------------+\n|                      Python Layer                           |\n|  - lunarphase/core/state.py (State Tracker Facade)          |\n+-------------------------------------------------------------+\n                              \u2502\n               PyO3 FFI Boundary (abi3-py39)\n                              \u2502\n                              \u25bc\n+-------------------------------------------------------------+\n|                Native Rust Extension Module                 |\n|                                                             |\n|  +-------------------------------------------------------+  |\n|  | StateTracker Struct                                   |  |\n|  |  - fields: FxHashMap&lt;String, FieldEntry&gt;               |  |\n|  +-------------------------------------------------------+  |\n|  | BatchAggregator Struct                                |  |\n|  |  - int_keys: FxHashSet&lt;i64&gt;                          |  |\n|  |  - str_keys: FxHashSet&lt;String&gt;                       |  |\n|  +-------------------------------------------------------+  |\n|  | QueryCompiler Struct                                  |  |\n|  |  - Buffer String Pre-allocation &amp; Dialect Placeholders  |  |\n|  +-------------------------------------------------------+  |\n|  | SchemaDiffEngine Struct                               |  |\n|  |  - Model vs Database Table Introspection Diffing       |  |\n|  +-------------------------------------------------------+  |\n+-------------------------------------------------------------+\n</code></pre>"},{"location":"architecture/#state-isolation-memory-layout","title":"State Isolation &amp; Memory Layout","text":""},{"location":"architecture/#statetracker-struct","title":"StateTracker Struct","text":"<p>The Rust <code>StateTracker</code> struct manages instance property state. Each field entry is encapsulated inside a single contiguous struct:</p> <pre><code>struct FieldEntry {\n    snapshot: String,  // Serialized state at hydration / commit\n    current: String,   // Current mutated state\n    py_obj: PyObject,  // Reference back to Python object\n}\n\n#[pyclass]\npub struct StateTracker {\n    fields: FxHashMap&lt;String, FieldEntry&gt;,\n}\n</code></pre>"},{"location":"architecture/#memory-allocation-impact","title":"Memory Allocation Impact","text":"<p>Standard Python ORM implementations store instance attribute graphs inside Python dictionaries (<code>__dict__</code>). For an object graph with $N$ instances and $M$ fields, Python allocates $N$ dictionary objects on the heap.</p> <p>LunarPhaseORM consolidates instance state into Rust-managed C structs utilizing <code>rustc_hash::FxHashMap</code> (a non-cryptographic hashing algorithm designed for short string keys). This reduces Python heap allocations by 66% per instance.</p>"},{"location":"architecture/#pure-python-fallback-system","title":"Pure Python Fallback System","text":"<p>If the native <code>_lunarphase_rs</code> extension module is unavailable in the execution environment, LunarPhaseORM automatically switches to a pure Python fallback implementation (<code>PyStateTracker</code>):</p> <pre><code># lunarphase/core/state.py\ntry:\n    from lunarphase._lunarphase_rs import StateTracker as RustStateTracker\n    HAS_RUST = True\nexcept ImportError:\n    HAS_RUST = False\n\nclass StateTrackerFacade:\n    def __init__(self):\n        if HAS_RUST:\n            self._impl = RustStateTracker()\n        else:\n            self._impl = PyStateTracker()\n</code></pre> <p>The fallback implementation maintains API equivalence, ensuring tests and execution continue without binary extensions, albeit without C-level memory compaction.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide details environment setup, database engine initialization, model definitions, and basic CRUD operations.</p>"},{"location":"getting-started/#installation","title":"Installation","text":"<p>Install the compiled package from PyPI:</p> <pre><code>pip install lunarphase-orm\n</code></pre>"},{"location":"getting-started/#local-build-from-source","title":"Local Build from Source","text":"<p>Building the C-extension requires Rust 1.80+ and Maturin:</p> <pre><code>git clone https://github.com/LunarPy-Labs/LunarPhaseORM.git\ncd LunarPhaseORM\npython3 -m venv .venv\nsource .venv/bin/activate\npip install maturin aiosqlite pydantic\nmaturin develop --release\n</code></pre>"},{"location":"getting-started/#database-connection-configuration","title":"Database Connection Configuration","text":"<p>LunarPhaseORM supports SQLite, PostgreSQL, and MySQL using async Python database drivers. Driver selection is determined by the connection URI scheme:</p> <pre><code>from lunarphase import create_engine\n\n# SQLite (In-Memory or File)\nsqlite_engine = create_engine(\"sqlite:///:memory:\")\n\n# PostgreSQL (via asyncpg)\npg_engine = create_engine(\"postgres://user:password@localhost:5432/dbname\")\n\n# MySQL (via aiomysql)\nmysql_engine = create_engine(\"mysql://user:password@localhost:3306/dbname\")\n</code></pre>"},{"location":"getting-started/#defining-models","title":"Defining Models","text":"<p>Models subclass <code>lunarphase.Model</code>. Column fields are defined using field descriptors:</p> <pre><code>from lunarphase import Model, PrimaryKeyField, StringField, IntegerField, BooleanField\n\nclass User(Model):\n    __tablename__ = \"users\"\n\n    id = PrimaryKeyField()\n    name = StringField(nullable=False)\n    age = IntegerField(default=18)\n    is_active = BooleanField(default=True)\n</code></pre>"},{"location":"getting-started/#basic-operations","title":"Basic Operations","text":""},{"location":"getting-started/#1-creating-records","title":"1. Creating Records","text":"<p>Records can be created using <code>Model.create()</code> or by instantiating the model class and calling <code>save()</code>:</p> <pre><code>async def create_examples():\n    # Approach 1: Direct instantiation and save\n    user = User(name=\"Alice\", age=25)\n    await user.save()\n\n    # Approach 2: Classmethod creation\n    bob = await User.create(name=\"Bob\", age=30)\n</code></pre>"},{"location":"getting-started/#2-querying-records","title":"2. Querying Records","text":"<p>The query builder API supports filtering, ordering, limiting, and pagination:</p> <pre><code>async def query_examples():\n    # Single record lookup\n    user = await User.where(id=1).first()\n\n    # Filtered dataset lookup\n    adults = await User.where(User.age &gt;= 18).order_by(\"age\", \"desc\").all()\n\n    # IN clause expansion\n    results = await User.where(id__in=[1, 2, 3]).all()\n</code></pre>"},{"location":"getting-started/#3-updating-records-dirty-field-tracking","title":"3. Updating Records (Dirty Field Tracking)","text":"<p>Modifying an attribute updates the model's internal tracking state. Calling <code>save()</code> generates an <code>UPDATE</code> query containing only the modified columns:</p> <pre><code>async def update_example():\n    user = await User.where(id=1).first()\n\n    # Modify property\n    user.age = 26\n\n    # Executes: UPDATE users SET age = 26 WHERE id = 1;\n    await user.save()\n</code></pre> <p>If <code>user.save()</code> is called without modifying attributes, no database query is executed.</p>"},{"location":"getting-started/#4-deleting-records","title":"4. Deleting Records","text":"<pre><code>async def delete_example():\n    user = await User.where(id=1).first()\n    await user.delete()\n</code></pre>"},{"location":"migrations-cli/","title":"Migrations &amp; CLI Reference","text":"<p>This document covers automated database schema diffing (<code>SchemaDiffEngine</code>), reversible migration generation, and terminal CLI commands.</p>"},{"location":"migrations-cli/#schema-diff-engine-schemadiffengine","title":"Schema Diff Engine (<code>SchemaDiffEngine</code>)","text":"<p>LunarPhaseORM includes an automated DDL generator powered by native Rust (<code>src/migration_diff.rs</code>). The engine compares in-memory <code>Model</code> field definitions against database table introspection schemas:</p> <pre><code>+---------------------------+       +---------------------------+\n|    Model Definitions      |       |  Database Introspection   |\n|  - id: INTEGER PRIMARY KEY|  vs   |  - id: INTEGER PRIMARY KEY|\n|  - name: VARCHAR(255)     |       |  - name: VARCHAR(255)     |\n|  - age: INTEGER           |       |  (missing 'age' column)   |\n+---------------------------+       +---------------------------+\n                                  \u2502\n                       SchemaDiffEngine.diff_table()\n                                  \u2502\n                                  \u25bc\n                Generated DDL: ALTER TABLE users ADD COLUMN age INTEGER;\n</code></pre>"},{"location":"migrations-cli/#command-line-tool-lunarphase","title":"Command Line Tool (<code>lunarphase</code>)","text":"<p>The <code>lunarphase</code> command-line interface provides tools to manage migration files, inspect status, apply pending migrations, and roll back changes.</p>"},{"location":"migrations-cli/#1-lunarphase-status","title":"1. <code>lunarphase status</code>","text":"<p>Displays current migration state and checks for pending migration files:</p> <pre><code>$ lunarphase status\nDatabase Dialect: sqlite\nCurrent Revision: 20260815_001_create_users_table.py\nPending Migrations: 1 file(s) remaining\n</code></pre>"},{"location":"migrations-cli/#2-lunarphase-makemigration","title":"2. <code>lunarphase make:migration</code>","text":"<p>Generates a reversible Python migration file in <code>migrations/</code>:</p> <pre><code>$ lunarphase make:migration \"add_user_age_column\"\nCreated migration: migrations/20260815_002_add_user_age_column.py\n</code></pre>"},{"location":"migrations-cli/#migration-file-anatomy","title":"Migration File Anatomy","text":"<p>Generated migration files export <code>upgrade()</code> and <code>downgrade()</code> functions:</p> <pre><code># migrations/20260815_002_add_user_age_column.py\n\nasync def upgrade(engine):\n    await engine.execute(\"ALTER TABLE users ADD COLUMN age INTEGER;\")\n\nasync def downgrade(engine):\n    await engine.execute(\"ALTER TABLE users DROP COLUMN age;\")\n</code></pre>"},{"location":"migrations-cli/#3-lunarphase-migrate","title":"3. <code>lunarphase migrate</code>","text":"<p>Applies all pending migration files sequentially:</p> <pre><code>$ lunarphase migrate\nApplying migration 20260815_002_add_user_age_column.py... OK\nSuccessfully applied 1 migration(s).\n</code></pre>"},{"location":"migrations-cli/#4-lunarphase-rollback","title":"4. <code>lunarphase rollback</code>","text":"<p>Rolls back the most recently applied migration by executing its <code>downgrade()</code> function:</p> <pre><code>$ lunarphase rollback\nRolling back migration 20260815_002_add_user_age_column.py... OK\nSuccessfully rolled back 1 migration(s).\n</code></pre>"},{"location":"models-and-fields/","title":"Models &amp; Fields","text":"<p>This document describes model definition mechanics, field descriptor types, metaclass attribute processing, and operator overloading expressions.</p>"},{"location":"models-and-fields/#model-metaclass-modelbase","title":"Model Metaclass (<code>ModelBase</code>)","text":"<p>Model classes derive from <code>lunarphase.Model</code>, which is instantiated by metaclass <code>ModelBase</code>. The metaclass performs class creation processing:</p> <ol> <li>Table Name Resolution: Reads <code>__tablename__</code>. If unspecified, defaults to the lowercase pluralized class name.</li> <li>Field Descriptor Collection: Scans class attributes for instances of <code>FieldDescriptor</code>, registering them into a <code>_fields</code> dictionary.</li> <li>Primary Key Identification: Verifies the presence of a <code>PrimaryKeyField</code>. If omitted, automatically appends <code>id = PrimaryKeyField()</code>.</li> <li>Relationship Registration: Collects relationship descriptors (<code>HasMany</code>, <code>BelongsTo</code>, <code>HasOne</code>) into <code>_relations</code>.</li> </ol>"},{"location":"models-and-fields/#field-descriptors","title":"Field Descriptors","text":"<p>Field descriptors manage type coercion, default value assignment, nullability constraints, and SQL data type mapping.</p>"},{"location":"models-and-fields/#available-field-types","title":"Available Field Types","text":"Field Class Python Type Default SQL Type Constructor Arguments <code>PrimaryKeyField</code> <code>int</code> <code>INTEGER PRIMARY KEY AUTOINCREMENT</code> <code>name=None</code> <code>StringField</code> <code>str</code> <code>VARCHAR(255)</code> <code>max_length=255</code>, <code>nullable=True</code>, <code>default=None</code> <code>IntegerField</code> <code>int</code> <code>INTEGER</code> <code>nullable=True</code>, <code>default=None</code> <code>FloatField</code> <code>float</code> <code>FLOAT</code> <code>nullable=True</code>, <code>default=None</code> <code>BooleanField</code> <code>bool</code> <code>BOOLEAN</code> <code>nullable=True</code>, <code>default=False</code> <code>DateTimeField</code> <code>datetime</code> <code>DATETIME</code> <code>auto_now=False</code>, <code>auto_now_add=False</code> <code>JSONField</code> <code>dict</code> / <code>list</code> <code>JSON</code> <code>nullable=True</code>, <code>default=None</code>"},{"location":"models-and-fields/#operator-overloading-ast-expressions","title":"Operator Overloading &amp; AST Expressions","text":"<p>Accessing a field descriptor at the class level returns a <code>ColumnRef</code> AST node. Operator overloading builds AST expressions without executing queries:</p> <pre><code># Returns ColumnRef(\"age\")\nUser.age\n\n# Returns BinaryOp(ColumnRef(\"age\"), \"&gt;=\", Literal(18))\nUser.age &gt;= 18\n</code></pre>"},{"location":"models-and-fields/#supported-operator-mappings","title":"Supported Operator Mappings","text":"Operator / Method SQL Output Pattern AST Node Formed <code>==</code> <code>column = ?</code> <code>BinaryOp(col, \"=\", val)</code> <code>!=</code> <code>column != ?</code> <code>BinaryOp(col, \"!=\", val)</code> <code>&lt;</code> <code>column &lt; ?</code> <code>BinaryOp(col, \"&lt;\", val)</code> <code>&lt;=</code> <code>column &lt;= ?</code> <code>BinaryOp(col, \"&lt;=\", val)</code> <code>&gt;</code> <code>column &gt; ?</code> <code>BinaryOp(col, \"&gt;\", val)</code> <code>&gt;=</code> <code>column &gt;= ?</code> <code>BinaryOp(col, \"&gt;=\", val)</code> <code>.in_(list)</code> <code>column IN (?, ?, ?)</code> <code>BinaryOp(col, \"IN\", tuple)</code> <code>.like(pattern)</code> <code>column LIKE ?</code> <code>BinaryOp(col, \"LIKE\", pattern)</code> <code>.is_null()</code> <code>column IS NULL</code> <code>BinaryOp(col, \"IS NULL\", None)</code> <code>.is_not_null()</code> <code>column IS NOT NULL</code> <code>BinaryOp(col, \"IS NOT NULL\", None)</code>"},{"location":"query-builder/","title":"Query Builder API","text":"<p>This document details the type-safe fluent <code>QueryBuilder</code> API, AST generation, dialect-specific parameter binding, and execution methods.</p>"},{"location":"query-builder/#query-construction","title":"Query Construction","text":"<p><code>QueryBuilder</code> constructs abstract syntax trees (AST) representing database queries. Method chaining appends clause nodes:</p> <pre><code>from lunarphase import QueryBuilder\n\n# Constructs AST for SELECT id, name FROM users WHERE age &gt;= 18 ORDER BY name ASC LIMIT 10\nquery = (\n    User.select(\"id\", \"name\")\n    .where(User.age &gt;= 18)\n    .order_by(\"name\", \"asc\")\n    .limit(10)\n)\n</code></pre>"},{"location":"query-builder/#dialect-parameter-binding","title":"Dialect Parameter Binding","text":"<p>SQL parameters are formatted according to the configured database engine driver:</p> Database Dialect Driver Package Parameter Placeholder Format Compiled Example <code>sqlite</code> <code>aiosqlite</code> <code>?</code> <code>WHERE age &gt;= ?</code> <code>postgres</code> / <code>postgresql</code> <code>asyncpg</code> <code>$1</code>, <code>$2</code>, <code>$N</code> <code>WHERE age &gt;= $1 AND status = $2</code> <code>mysql</code> <code>aiomysql</code> <code>%s</code> <code>WHERE age &gt;= %s</code> <p>Parameter placeholders are generated in Rust via <code>QueryCompiler::get_placeholder(dialect, index)</code>.</p>"},{"location":"query-builder/#query-execution-methods","title":"Query Execution Methods","text":"Method Signature Return Type Description <code>await query.all()</code> <code>List[Model]</code> Fetches all matching rows and hydrates model instances. <code>await query.first()</code> <code>Optional[Model]</code> Fetches the first matching record or returns <code>None</code>. <code>await query.count()</code> <code>int</code> Executes <code>SELECT COUNT(*) FROM table WHERE ...</code> and returns integer count. <code>await query.exists()</code> <code>bool</code> Evaluates if at least one matching record exists in the table. <code>await query.paginate(page, per_page)</code> <code>Tuple[List[Model], int]</code> Applies <code>LIMIT</code> and <code>OFFSET</code> for pagination, returning records and total count."},{"location":"query-builder/#in-expansion-__in","title":"IN Expansion (<code>__in</code>)","text":"<p>Passing list parameters expands placeholders dynamically:</p> <pre><code># Executes: SELECT * FROM users WHERE id IN (?, ?, ?)\nusers = await User.where(id__in=[1, 2, 3]).all()\n</code></pre>"},{"location":"relations-and-batching/","title":"Relations &amp; Auto-Batching Engine","text":"<p>This document describes relationship descriptors (<code>HasMany</code>, <code>BelongsTo</code>, <code>HasOne</code>), eager loading, and the <code>DeferredAutoBatcher</code> micro-task scheduling algorithm.</p>"},{"location":"relations-and-batching/#defining-relationships","title":"Defining Relationships","text":"<p>Relationships are declared on models using relationship descriptors:</p> <pre><code>from lunarphase import Model, PrimaryKeyField, StringField, IntegerField, HasMany, BelongsTo\n\nclass Author(Model):\n    __tablename__ = \"authors\"\n    id = PrimaryKeyField()\n    name = StringField()\n    posts = HasMany(lambda: Post, foreign_key=\"author_id\")\n\nclass Post(Model):\n    __tablename__ = \"posts\"\n    id = PrimaryKeyField()\n    title = StringField()\n    author_id = IntegerField()\n    author = BelongsTo(Author, foreign_key=\"author_id\")\n</code></pre>"},{"location":"relations-and-batching/#the-n1-problem-solution-algorithm","title":"The N+1 Problem &amp; Solution Algorithm","text":"<p>When accessing relationship attributes inside an async iteration loop, naive ORM implementations issue a separate SQL <code>SELECT</code> for each parent model ($N+1$ queries):</p> <pre><code># Naive execution issuing N+1 queries:\nauthors = await Author.all()\nfor author in authors:\n    posts = await author.posts # Issues SELECT * FROM posts WHERE author_id = ? for EVERY loop!\n</code></pre>"},{"location":"relations-and-batching/#deferredautobatcher-execution-cycle","title":"<code>DeferredAutoBatcher</code> Execution Cycle","text":"<p>LunarPhaseORM resolves $N+1$ queries by scheduling relationship loading tasks into the <code>asyncio</code> event loop micro-task queue via <code>loop.call_soon()</code>:</p> <pre><code>Step 1: Loop Execution\n  for author in authors:\n      posts = await author.posts \u2500\u2500\u25ba Registers key into DeferredAutoBatcher queue\n\nStep 2: Micro-Task Scheduler (loop.call_soon)\n  Event loop yields control to micro-task \u2500\u2500\u25ba Triggers _flush_batch()\n\nStep 3: Single Batch Query Execution\n  Executes ONE SQL query: SELECT * FROM posts WHERE author_id IN (1, 2, 3, ... N);\n\nStep 4: Rust Key Deduplication &amp; Result Distribution\n  BatchAggregator deduplicates keys via FxHashSet&lt;i64&gt; \u2500\u2500\u25ba Resolves Future for each author\n</code></pre>"},{"location":"relations-and-batching/#eager-loading-with_relations","title":"Eager Loading (<code>with_relations</code>)","text":"<p>Relationships can also be eagerly fetched in advance using <code>with_relations()</code>:</p> <pre><code># Fetches authors and eagerly loads all related posts in a single batch query\nauthors = await Author.with_relations(\"posts\").all()\n\nfor author in authors:\n    for post in author.posts:\n        print(post.title)\n</code></pre>"},{"location":"unit-of-work/","title":"Unit of Work &amp; Transactions","text":"<p>This document details session management, identity mapping (<code>IdentityMap</code>), atomic transaction boundaries (<code>UnitOfWork</code>), and snapshot isolation.</p>"},{"location":"unit-of-work/#identity-map-identitymap","title":"Identity Map (<code>IdentityMap</code>)","text":"<p>The <code>IdentityMap</code> maintains in-memory object identity within a session scope, ensuring that multiple queries fetching the same database record (<code>table</code>, <code>primary_key</code>) return references to the exact same Python object instance:</p> <pre><code>from lunarphase.core.uow import IdentityMap\n\nidentity_map = IdentityMap()\n\n# Registers or returns existing instance\nuser1 = identity_map.get(User, 1)\nuser2 = identity_map.get(User, 1)\n\nassert user1 is user2  # Guarantees single object identity in memory\n</code></pre>"},{"location":"unit-of-work/#transaction-engine-unitofwork","title":"Transaction Engine (<code>UnitOfWork</code>)","text":"<p><code>UnitOfWork</code> tracks pending mutations across model instances during a session, committing changes atomically inside transaction blocks.</p>"},{"location":"unit-of-work/#basic-transaction-scope","title":"Basic Transaction Scope","text":"<pre><code>from lunarphase import UnitOfWork, create_engine\n\nengine = create_engine(\"sqlite:///app.db\")\nsession = UnitOfWork(engine)\n\nasync with session.begin():\n    user = User(name=\"Alice\", age=25)\n    session.register_new(user)\n\n    order = Order(user_id=user.id, amount=100.0)\n    session.register_new(order)\n\n    # Exiting context block automatically calls await session.commit()\n</code></pre> <p>If an exception occurs within <code>async with session.begin():</code>, the context manager automatically catches the error, calls <code>await session.rollback()</code>, and propagates the exception.</p>"},{"location":"unit-of-work/#dirty-state-tracking-mechanism","title":"Dirty State Tracking Mechanism","text":"<pre><code>State Initialization (hydrate / create)\n  \u2502\n  \u251c\u2500\u25ba Snapshot State: {\"name\": \"Alice\", \"age\": 25}\n  \u2514\u2500\u25ba Current State:  {\"name\": \"Alice\", \"age\": 25}\n\nAttribute Mutation: user.age = 26\n  \u2502\n  \u251c\u2500\u25ba Snapshot State: {\"name\": \"Alice\", \"age\": 25}\n  \u2514\u2500\u25ba Current State:  {\"name\": \"Alice\", \"age\": 26}  &lt;-- Marked Dirty!\n\nCalling user.save() / session.commit()\n  \u2502\n  \u251c\u2500\u25ba Rust StateTracker diffs FxHashMap entries\n  \u251c\u2500\u25ba Formats SQL: UPDATE users SET age = 26 WHERE id = 1;\n  \u2514\u2500\u25ba Calls hydrate_snapshot(): Snapshot updated to {\"name\": \"Alice\", \"age\": 26}\n</code></pre>"}]}